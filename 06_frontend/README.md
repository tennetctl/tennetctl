# 06_frontend

tennetctl Next.js 15 (App Router) frontend with Tailwind CSS v4.

## Run locally

```bash
cd 06_frontend
npm install
npm run dev
# → http://localhost:3000
```

## Run via docker compose

```bash
docker compose up frontend
# → http://localhost:3000
```
