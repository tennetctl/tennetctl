# E2E Test Credentials

These credentials were used during manual E2E testing with Playwright MCP.
**Do not commit this file to version control.**

## First Admin User

| Field    | Value           |
|----------|-----------------|
| Username | admin           |
| Email    | admin@example.com |
| Password | ChangeMe123!    |

## Notes

- Password is the default set by `tennetctl setup --yes` (phase 3 `--yes` flag default)
- After a fresh install, change this password immediately
- These are dev-only credentials — never use in staging or production
