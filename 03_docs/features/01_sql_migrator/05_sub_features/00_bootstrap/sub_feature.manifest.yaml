title: "SQL Migrator — Schema Bootstrap"
sub_feature: "00_bootstrap"
feature: "01_sql_migrator"
status: BUILDING
owner: "srigaddeks"
created_at: "2026-04-08"
description: |
  Creates the 00_schema_migrations Postgres schema and the applied_migrations
  tracking table. Applied by hand exactly once on a fresh database, before the
  Python runner exists. After this migration is in place, all subsequent
  migrations are applied via `uv run python -m scripts.migrate up`.
